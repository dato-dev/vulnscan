# Клиент Prometheus. Отдельно от трассировки, потому что метрики нужны всем
# образам, включая те, где OTel не при чём: зеркало баз и пример подключения.
#
# Версия одна на все образы намеренно. Разъехавшиеся версии клиента дают
# разный формат экспозиции, и расхождение проявится не ошибкой, а тем, что
# Prometheus перестанет разбирать часть рядов.
prometheus-client==0.26.0
