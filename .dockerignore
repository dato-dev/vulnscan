# Контекст сборки. Всё, что не перечислено в COPY, образу не нужно — а
# лишний контекст это и время, и риск утащить в слой то, чего там быть
# не должно.
#
# Образам нужны ровно: packages/vscommon, rules, pyproject.toml и каталог
# своего сервиса. Остальное сюда не едет.

# ─── Секреты. Никогда. ───────────────────────────────────────────────
#
# Конфигурация монтируется на запуске, а не встраивается в образ:
# перевыпуск ключа не должен требовать пересборки.
.env
*.env
.env.*
keys.json
proxy.env
*secret*
*.pem
*.key
policies.json
weights.json
deploy/

# ─── Окружение и кэши ────────────────────────────────────────────────
.venv/
.git/
.github/
.pytest_cache/
.mypy_cache/
.ruff_cache/
__pycache__/
*.py[cod]
.coverage
htmlcov/

# ─── Не нужно в рантайме ─────────────────────────────────────────────
tests/
bench/
docs/
samples/
corpus/
badpdfgen/
*.md

# ─── Мусор ───────────────────────────────────────────────────────────
.DS_Store
._*
.idea/
.vscode/
*.log
